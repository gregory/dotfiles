export SHELL=/bin/zsh
export WORK_PATHS=$HOME/code:$HOME/code/voxi
export PATH="$HOME/.rbenv/bin:/usr/local/bin:/usr/local/sbin:~/bin:$PATH"
export DOCKER_TLS_VERIFY=1
export DOCKER_CERT_PATH=/Users/gregory/.boot2docker/certs/boot2docker-vm

export LESS='-RNf'
export LESSPIPE=/usr/local/bin/src-hilite-lesspipe.sh
export LESSOPEN="| ${LESSPIPE} %s"
alias docker="docker -H $DOCKER_HOST"
alias less='less -m -N -g -i -J --underline-special --SILENT'

eval "$(rbenv init -)"
